"""One suite per strategy specification (specification section 27).

Distinct from ``tests/strategy``, which tests the strategy *stage* — the
registry, the agent boundary and the service. These files test what each
individual strategy is and how it resolves to contracts.
"""
