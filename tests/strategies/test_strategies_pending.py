"""Strategy suites — not yet implemented.

Delivered in Milestone 6.

Per-strategy tests for entry validity, DTE boundaries, strike selection, liquidity
filters, IV constraints, multi-leg construction and strategy-level exit.

This placeholder keeps the suite discoverable and makes the gap visible in the
test report instead of silently absent.
"""

from __future__ import annotations

import pytest


@pytest.mark.skip(reason="Strategy suites are delivered in Milestone 6")
def test_suite_pending() -> None:
    raise AssertionError("placeholder; never executed")
