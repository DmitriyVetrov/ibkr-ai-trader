"""The strategy-selection stage (Milestone 6).

Distinct from ``tests/strategies``, which holds one suite per individual
strategy specification (specification section 27). This directory tests the
*stage*: the registry, the agent boundary, the deterministic validation and the
service that drives them.
"""
